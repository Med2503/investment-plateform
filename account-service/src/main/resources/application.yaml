server:
  port: 8083

spring:
  application:
    name: account-service

  datasource:
    url: jdbc:postgresql://localhost:5432/account_db
    username: bank_user
    password: bank_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
